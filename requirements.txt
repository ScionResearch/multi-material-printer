# Python dependencies for Scion Multi-Material Unit Controller
# Install with: pip install -r requirements.txt

# Network communication
requests>=2.25.0
urllib3>=1.26.0

# Configuration file handling
configparser>=5.0.0

# GPIO and hardware control (Raspberry Pi)
RPi.GPIO>=0.7.0; platform_machine=="armv7l" or platform_machine=="aarch64"
gpiozero>=1.6.0; platform_machine=="armv7l" or platform_machine=="aarch64"

# Adafruit motor control libraries
adafruit-circuitpython-motorkit>=1.6.0; platform_machine=="armv7l" or platform_machine=="aarch64"
adafruit-circuitpython-motor>=3.4.0; platform_machine=="armv7l" or platform_machine=="aarch64"

# JSON processing (usually included with Python)
# json - standard library

# For development and testing
pytest>=6.0.0
mock>=4.0.0

# Optional: for advanced logging
colorlog>=6.0.0